import { ColumnSumPipe } from './column-sum.pipe';

describe('ColumnSumPipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnSumPipe();
    expect(pipe).toBeTruthy();
  });
});
