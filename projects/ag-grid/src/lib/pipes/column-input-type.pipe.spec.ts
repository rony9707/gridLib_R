import { ColumnInputTypePipe } from './column-input-type.pipe';

describe('ColumnInputTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnInputTypePipe();
    expect(pipe).toBeTruthy();
  });
});
