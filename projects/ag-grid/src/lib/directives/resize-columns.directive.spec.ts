import { ResizeColumnsDirective } from './resize-columns.directive';

describe('ResizeColumnsDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeColumnsDirective();
    expect(directive).toBeTruthy();
  });
});
